{
  "_comment": "Production Secondary Server Configuration",
  "_description": "Use this configuration on the secondary HA server (e.g., 192.168.1.101)",
  "_instructions": [
    "1. Copy this file to /opt/trapninja/config/ha_config.json",
    "2. Update peer_host with the IP address of your primary server",
    "3. Keep priority lower than primary (ensures proper failover)",
    "4. Set enabled to true when ready to activate HA",
    "5. Restart TrapNinja service: python trapninja.py --restart"
  ],
  "enabled": false,
  "mode": "secondary",
  "peer_host": "192.168.1.100",
  "peer_port": 8162,
  "listen_port": 8162,
  "heartbeat_interval": 1.0,
  "heartbeat_timeout": 3.0,
  "failover_delay": 2.0,
  "priority": 100,
  "shared_secret": "",
  "split_brain_detection": true,
  "max_retries": 3,
  "retry_delay": 0.5
}
